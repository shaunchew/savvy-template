---
name: Bug report
about: Something broke, misbehaved, or touched files it shouldn't have
labels: bug
---

## What happened

<!-- What did you run, what did you expect, what actually happened -->

## Did the framework modify or delete files it shouldn't have?

<!-- If yes: which files? This gets the `safety` label and release-blocker
     treatment. Include the .savvy-old backup diff if you have one. -->

## Environment

- OS:
- Claude Code version (`claude --version`):
- Framework version (`.claude/config.toml` or `/plugin` list):
- Adopted via: `/sf:adopt` plugin / legacy Copier template / manual

## Reproduction

```bash
# minimal steps, ideally against a throwaway git repo
```
