#!/usr/bin/env bash
set -euo pipefail
# session-end.sh — invokes /handover, surfaces pending-changes count, optionally prompts for /lesson. Stop hook.
# Stub — implementation pending. See docs/PLAN.md §3.5.
exit 0
