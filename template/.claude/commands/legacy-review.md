---
description: Walk _legacy/<migration>/ items deciding Keep/Delete/Restore; --initial-migration scans repo at first adoption.
argument-hint: "[--initial-migration]"
---

# /legacy-review

Walk `_legacy/<migration>/`. Per item: Keep / Delete / Restore. `--initial-migration` scans repo for non-conforming files at first adoption. Invokes: legacy-reviewer.

## Status

Stub — content to be filled in Phase 1 implementation. See `docs/PLAN.md` §7 for the full spec.
