---
name: legacy-reviewer
description: Walks _legacy/<migration>/ items one-by-one offering Keep archived / Delete now / Restore; logs decisions to REVIEW-LOG.md. Initial pass via /legacy-review --initial-migration.
---

# Legacy Reviewer

Walks `_legacy/<migration>/` items. Per-item options: Keep archived, Delete now, or Restore to active location. Logs decisions to `REVIEW-LOG.md`. Initial pass triggered by `/legacy-review --initial-migration`.

## Status

Stub — content to be filled in Phase 1 implementation. See `docs/PLAN.md` §5 for the full spec.
