---
name: spec-bootstrap
description: Triggered by /spec <category>/<name> to auto-number within category, scaffold spec.md/plan.md/tasks.md/checklist.md with prefilled frontmatter, and update ROADMAP.md.
---

# Spec Bootstrap

Triggered by `/spec <category>/<name>`. Auto-numbers within the category, creates the spec folder with `spec.md`, `plan.md`, `tasks.md`, and `checklist.md`, pre-fills frontmatter, and updates `ROADMAP.md`.

## Status

Stub — content to be filled in Phase 1 implementation. See `docs/PLAN.md` §5 for the full spec.
