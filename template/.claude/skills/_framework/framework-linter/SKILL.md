---
name: framework-linter
description: Run on-demand via /lint-framework to detect drift, missing files, malformed specs, rule violations, template version drift, stale _legacy/ entries, and stale scratchpads.
---

# Framework Linter

On-demand checker invoked via `/lint-framework`. Identifies drift, missing files, malformed specs, rule violations, template version drift, stale `_legacy/` entries (>90 days without full review), and stale scratchpads (>60 days without promotion or archive).

## Status

Stub — content to be filled in Phase 1 implementation. See `docs/PLAN.md` §5 for the full spec.
